export default class Rocket {

  constructor(name, speed, teamNumber, icon) {
      this.name = name
      this.speed = speed
      this.teamNumber = teamNumber
      this.icon = icon
  }

  launch() {
    // to do
  }
}
